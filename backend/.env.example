# ============================================================
# BUDGET MANAGER - ENVIRONMENT CONFIGURATION
# ============================================================
# Copy this file to .env and fill in your values
# ============================================================

# ============================================================
# APPLICATION SETTINGS
# ============================================================
PORT=3000
NODE_ENV=development

# ============================================================
# DATABASE CONFIGURATION
# ============================================================
# Choose your database setup below:

# OPTION 1: Docker PostgreSQL (Local Development - DEFAULT)
# Use this for local development with Docker Compose
DATABASE_URL="postgresql://postgres:postgres@db:5432/budget_management?schema=public"

# OPTION 2: Supabase (Cloud Database)
# Use this for production or cloud deployment
# Replace YOUR_SUPABASE_USER and YOUR_SUPABASE_PASSWORD with actual values
# DATABASE_URL="postgresql://postgres.YOUR_PROJECT_ID:YOUR_SUPABASE_PASSWORD@aws-1-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true&schema=public"
# DIRECT_URL="postgresql://postgres.YOUR_PROJECT_ID:YOUR_SUPABASE_PASSWORD@aws-1-us-east-1.pooler.supabase.com:5432/postgres?schema=public"

# OPTION 3: Local PostgreSQL (Standalone - Not Docker)
# Use this if you have PostgreSQL installed locally
# DATABASE_URL="postgresql://postgres:YOUR_LOCAL_PASSWORD@localhost:5432/budget_management?schema=public"

# ============================================================
# LOGGING (Optional)
# ============================================================
# LOG_LEVEL=debug
# LOG_FORMAT=json

# ============================================================
# CORS SETTINGS (For Frontend Communication)
# ============================================================
# Comma-separated list of allowed origins
# CORS_ORIGINS=http://localhost:3001,http://localhost:3000

# ============================================================
# INSTRUCTIONS FOR DATABASE SWITCHING
# ============================================================
#
# TO USE DOCKER POSTGRESQL (LOCAL - RECOMMENDED FOR DEVELOPMENT):
# 1. Keep DATABASE_URL as: postgresql://postgres:postgres@db:5432/budget_management?schema=public
# 2. Run: docker-compose up --build
# 3. The database is automatically created
#
# TO USE SUPABASE (PRODUCTION/CLOUD):
# 1. Go to https://supabase.com and create a new project
# 2. Get your connection details:
#    - Project ID: visible in project URL
#    - Database password: set during project creation (or reset in Settings)
# 3. Update DATABASE_URL with your Supabase credentials
# 4. Run the SQL setup script (SUPABASE_SETUP.sql) in Supabase SQL Editor
# 5. Run: npx prisma db push
# 6. Start the application: npm run dev
#
# TO USE LOCAL POSTGRESQL (WITHOUT DOCKER):
# 1. Install PostgreSQL locally
# 2. Create a database: createdb budget_management
# 3. Update DATABASE_URL with your local credentials
# 4. Run: npx prisma migrate dev --name init
# 5. Start the application: npm run dev
#
# ============================================================
